import React, {Component} from "react";

class EntityMultiSelect extends Component {
    render(): React.ReactNode {
        const { classes, items, onDeselect, ...other } = this.props;
        return undefined;
    }


}

export default EntityMultiSelect;